/* import { signOut } from "firebase/auth";
import { auth } from './firebase.js';


const logout = document.querySelector('########')

logout.addEventListener('click', async() =>{
    await signOut(auth)
    console.log('Usted ha cerrado la sesión');
})
 */